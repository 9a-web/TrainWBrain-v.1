# PRD — TrainWithBrain (память агента)

> Канонические документы проекта: **`/app/twb_plan.md`** (ТЗ + архитектура + дорожная карта) и **`/app/AI_CONTEXT.md`** (техническая карта кода). Этот файл — краткая память для агентов.

## Проблема / продукт
Приложение для трекинга силовых тренировок (пауэрлифтинг) с двумя ролями — **спортсмен** и **тренер**. Работает как сайт, Telegram Mini App и PWA. Тренер ведёт подопечных, готовит планы, в реальном времени видит и подтверждает тренировку; спортсмен запускает тренировку и по-подходно отмечает выполнение.

## Стек
React 19 (CRACO) + FastAPI + MongoDB (Motor). Backend — монолит `server.py` (~3400 строк) + `models.py`/`auth.py`/`realtime.py`/`seed.py`. Frontend — `src/` (App, api, components, pages, context, hooks).

## Реализовано (актуально на Июнь 2026)
- **Auth (обязательный):** email/пароль (bcrypt), Telegram initData (HMAC), Google (прямой OAuth + Emergent). Сессии `user_sessions`, Bearer `twb_token`. **IDOR закрыт** на `/sessions/*` и `/plans/*` (401/403).
- **P1** Программы (29 упражнений, 4 шаблона), снимок плана, реальный прогресс.
- **P2** Жизненный цикл тренировки, **по-подходный лог + таймер отдыха + настройки**, тоннаж/%1ПМ/streak.
- **P2.1** Пропуски/переносы (`plan_day_marks`), adherence.
- **P3** Режим тренера: invite/link, подопечные, редактор плана, draft/published, недели, тренировочные дни, **подтверждение тренером**, **старт тренировки тренером**.
- **P4** Real-time WebSocket (`/api/ws`), co-scribe (`filled_by`/`coach_confirmed`), `useRealtime`.
- **P7** Подробная статистика с графиками (recharts), coach-gated эндпоинты.
- **CP** Кроссплатформенность: website + Telegram + PWA.

## Сделано в этой сессии (Июнь 2026)
- Переписан `AI_CONTEXT.md` v4.0 под актуальный код; синхронизирован `twb_plan.md` (P4/auth/IDOR/по-подходный лог → ✅).
- Прогнан фронтенд E2E Phases 2–4 (Coach Live: подтверждение + старт тренером + co-scribe) — 100% зелёное (`test_reports/iteration_1.json`).
- 3 UX-правки (по замечаниям юзабилити) + верификация (`iteration_2.json`, PASS):
  1. Home спортсмена: чип «Подтверждена тренером» вместо кнопки «Продолжить» при `coach_confirmed`.
  2. Тумблер подтверждения тренера получил текстовую подпись «Подтвердить»/«Подтв.».
  3. Активная карточка упражнения авто-раскрывается после загрузки данных сессии.

## Статистика: багфиксы B1–B11 + редизайн (Июнь 2026, эта сессия)
- **B1 IDOR закрыт на `/stats/*`:** все 4 роута `/stats/{tg}*` требуют `Depends(get_current_user)` + `_assert_can_read_stats` (владелец или активный тренер); coach-gated `/coach/{coach_id}/clients/{ath}/stats|exercise-progress` проверяют caller==coach_id.
- **B2/B9:** единый анкер даты `_session_day(s)` (date → finished_at) во всех расчётах; сессии без даты исключаются из range-фильтра.
- **B3:** `_est_1rm` — формула Эпли по фактическим весу/повторам (fallback: вес ÷ план%). 1ПМ/мышцы в detailed считаются свежо из упражнений (не из замороженного снимка).
- **B4:** exercise-progress без `plan_id` = «всё время» (все планы, группировка по датам, `scope: all`); с `plan_id` — по микроциклам.
- **B5:** в списке упражнений только те, где есть рабочий вес.
- **B6:** `avg_per_week` уважает скоуп (на микроцикл при plan_id, иначе по календарю).
- **B7:** `completion_pct`/`volume_pct` ограничены 100%.
- **B8:** подсобные упражнения учитываются в распределении по группам (≥1 подход).
- **B10:** подпись «по микроциклам» только при реальном плане (frontend).
- **B11:** streak `total_workouts` = число сессий (как в Stats) + новое поле `active_days`; в календаре `count` на день.
- **Редизайн `/stats`:** bento-hero (тоннаж + спарклайн, кольцо выполнения, серия, мини-KPI), 1ПМ-карточки факт(Эпли)/план с прогресс-баром, карточка «Динамика» (табы тоннаж/частота, ReferenceLine среднего), «Прогресс упражнения» с метрик-чипами (Вес/1ПМ/Тоннаж, AreaChart с градиентом), донат групп мышц с %-легендой, skeleton-загрузка, ambient-glow, GG Zaglav для больших чисел.
- **Редизайн `/streak`:** hero с гигантским числом (GG Zaglav, градиент, glow), подстаты-карточки, прогресс-бар цели недели, хитмап с метками месяцев и интенсивностью (count≥2 = hot), выровненные лейблы дней.
- **Инфраструктура:** `frontend/.env` REACT_APP_BACKEND_URL обновлён под текущий контейнер (7fa95d68-…); демо-аккаунт `statsdemo@twb.dev` (скрипт `/app/scripts/setup_stats_demo.py`).
- **Тестирование:** testing agent — 23/23 backend pytest PASS, frontend 100%, регрессий нет (`test_reports/iteration_4.json`; suite `/app/backend/tests/test_stats.py`).

## P6: Импорт своих программ (Июнь 2026, эта сессия)
- **Конструктор** (`/programs/builder/:id`): недели → дни → упражнения; выбор из каталога (29 + свои через модалку создания), схемы подходов (вес×подходы×повторы), отдых, подсобные, lift_group; дублирование недель/дней; перемещение упражнений; автосохранение (debounce 900мс, PATCH) с индикатором.
- **Шаринг и импорт по коду/ссылке:** POST `/programs/templates/{id}/share` → стабильный код `TWB-XXXXXX` + web-ссылка `/import/{code}` + Telegram deep-link (`t.me/TrainWbrain_bot?startapp=import_TWB_XXXXXX`, username бота через getMe с кэшем). Публичное превью `GET /programs/shared/{code}` (нормализация кода: регистр/подчёркивания/без префикса). `POST /programs/import/{code}` — копия в «Мои программы» (dedup по shared_from, own=true для своей).
- **Landing `/import/{code}`:** доступна БЕЗ входа; превью программы + выбор платформы (сайт / Telegram); «Продолжить на сайте» сохраняет `twb_pending_import` → после логина авто-редирект обратно и импорт; Telegram start_param `import_*` тоже обрабатывается (App.js).
  - **Мини-превью 1-й недели + прогноз (Июль 2026):** карточки дней (день + список упражнений с мышцей/схемой N×повт @ вес/%). Прогноз тоннажа (сумма вес×подходы×повт по неделям) с SVG-спарклайном и Δ %. Для программ на %1ПМ (`requires_maxes=true`) — интенсивность (доля от `base_maxes[lift_group]`) в п.п. вместо тоннажа. Бэкенд: `_template_first_week_preview`, `_template_forecast` в `server.py`.
- **ИИ-анализ** (`/programs/ai`, DeepSeek V4 Flash через **RouterAI**): генерация по промпту + разбор готового плана (текст / файл .xlsx/.csv/.txt через openpyxl). OpenAI-совместимый клиент на httpx, конфиг из .env: `AI_BASE_URL=https://routerai.ru/api/v1`, `AI_API_KEY`, `AI_MODEL=deepseek/deepseek-v4-flash`. ✅ АКТИВИРОВАНО — end-to-end проверено: 3-нед. программа с прогрессией +2.5 кг, fuzzy-матчинг на каталог упражнений (difflib). Проценты 1ПМ маппятся через base_maxes=100 → масштабирование под максимумы пользователя работает из коробки. **Июнь 2026:** системный промпт генерации усилен (`_AI_SYSTEM_GEN`): 5–8 упражнений/день (1–2 базовых + 3–6 подсобки), недельный объём 10–20 подходов на группу, обязательная прогрессия по неделям + разгрузка, выбор сплита по числу дней (full-body / верх-низ / PPL), русские названия под каталог; `max_tokens` 8000→16000 (компактный JSON). Проверено: 4 нед × 4 дня × 6–7 упр, волна 70→75→80→65%. **Двухшаговая генерация:** `POST /api/ai/program/questions` — ИИ генерирует 3–5 уточняющих вопросов с вариантами по первому промпту; фронт (AiImport.js) показывает чипсы + поле «свой ответ» + кнопку «Сгенерировать без ответов»; ответы передаются в `generate` полем `answers[{question,answer}]`. **Фоновые ИИ-задачи:** generate/parse/parse-file возвращают `{job_id}` мгновенно (ingress рубит запросы >60 c), выполняются через asyncio.create_task, статус — `GET /api/ai/program/jobs/{job_id}` (коллекция `ai_jobs`), фронт поллит каждые 3 c с дедлайном 5 мин. E2E проверено в браузере: вопросы → ответы → busy → результат (30 упражнений сохранены). Pytest 27/27. **Июль 2026 (эта сессия):** (1) Фикс «ИИ вернул некорректный JSON»: `_extract_json` теперь чинит ответ через `json_repair` (markdown-обёртки, обрезанный JSON, висячие запятые) + логирует сырой ответ при провале. Корневая причина обрезания: модель reasoning-типа, рассуждения съедают бюджет `max_tokens` → лимит поднят 16000→32000 (RouterAI поддерживает). (2) Системный промпт переписан на научной базе (мета-анализы Schoenfeld, Wendler 5/3/1, RIR): объём 10–20 подходов/мышцу/нед (макс 6–8 тяжёлых за сессию), частота 2×/нед на мышцу, изоляция до 0–2 RIR / база 2–4 RIR, волновая периодизация 5/3/1 (65/75/85→70/80/90→75/85/95→deload 40–60%), deload каждая 4-я неделя, отдых 150–240с база / 60–120с изоляция, русское название программы. E2E: 4 нед × 4 дня × 7 упр, 81 подход/нед, точная волна 5/3/1 + deload с половинным объёмом. **Графики прогресса после генерации:** компонент `AiProgressChart.js` (recharts AreaChart) на экране результата — чипы «Общий (средний)» + каждое упражнение с весами; общий = средний рост топ-веса всех упражнений (неделя 1 = 100%), бейдж «+X% к пику» (пик, а не финал — deload не искажает); per-exercise = топ-вес по неделям (кг или %1ПМ для requires_maxes). Проверено скриншот-тестом с моком generate + реальным поллингом job.
- **Programs.js:** секция «Мои программы» (бейджи Конструктор/Импорт/ИИ, действия изменить/поделиться/удалить с двухшаговым подтверждением), 3 кнопки создания, модалки шаринга и импорта по коду.
- **Безопасность:** все template-роуты auth-based; PATCH/DELETE/share только владельцу (403); списки = builtins + свои. Тренер видит свои программы в списке назначения (CoachClient) — может назначить подопечному.
- **Тестирование:** testing agent — 25/25 backend pytest PASS, frontend E2E 100% (`test_reports/iteration_5.json`; suite `/app/backend/tests/test_programs_p6.py`). Демо: шаблон `b85dbff0-…`, код `TWB-ZZB5ZS`.

## Бэклог / приоритеты
- **P1:** счётчик импортов `imports_count` + бейдж на публичном превью шаринга.
- **P1 (рекомендация):** сократить путь тренера к LIVE-экрану (ярлык «Кабинет тренера» на Home для coach-режима).
- **P5** Доступ по неделям (`week_access`) + опц. оплата (Telegram Stars/Stripe) — не начато.
- **P6** UI-загрузка файла (JSON/CSV/Excel), импорт по коду/ссылке, экспорт, конструктор — впереди.
- **Рефакторинг (P2):** разбить `server.py` по роутерам; `WorkoutView.js` (>1000 строк) на модули; фикс warning `useCallback` deps в `CoachPlanEditor.js` (missing `selectedWeek`).

## Июль 2026 (эта сессия) — Расширенные вопросы ИИ + анимация обдумывания + красивый просмотр программы
- `_AI_QUESTIONS_SYSTEM` переписан: **8–10 вопросов** с полем `tier: basic|advanced`. **Явный запрет спрашивать про 1ПМ** — максимумы вводятся при выборе программы после генерации, программы на %1ПМ автоматически масштабируются под введённые максимумы. Обязательно покрываются: цель, уровень, дни/нед, оборудование, длительность, время (basic); травмы, сплит, любимые/нелюбимые упражнения, кардио, питание, восстановление, стиль прогрессии (advanced). ИИ пропускает вопросы, если ответ уже есть в промпте.
- `/api/ai/program/questions` возвращает `tier` (default: basic). Лимит 10 вопросов, 10 ответов в `/generate`.
- `AiImport.js` — **двухшаговый флоу** (`qStep: basic|advanced`): степпер «1 · Основное → 2 · Тонкая настройка», индикация активного/пройденного шага. Кнопки: `Далее` (basic → advanced) / `Сгенерировать без продвинутых` (шорткат) / `Сгенерировать программу` (финал) / `Сгенерировать без ответов` / `← Назад` / `← Изменить запрос`. Если ИИ вернул только basic-вопросы — степпер скрыт, работает как одностраничный флоу.
- **Анимация обдумывания:** `BusyOverlay` использует `@paper-design/shaders-react` `GrainGradient` (shape `wave`, фирменная палитра #ff8a24 → #ffda24 → #ff5e00 → тёмный, `intensity=0.55`, `noise=0.35`) как живой фон + glass-orb `Sparkles` поверх. Vignette-накладка для читаемости текста. Работает и на этапе «Изучаем ваш запрос…», и на этапе «Строим недели и дни…».
- **Красивый просмотр программы:** `/app/frontend/src/components/AiProgramPreview.js` + `.css` — модалка bottom-sheet стиля с недельными табами (1/2/3/4/…), карточками дней (день + бейдж дня недели + плановый тоннаж/интенсивность), карточками упражнений (цветной левый маркер по группе мышц, чипы `ЖИМ/ПРИСЕД/ТЯГА` для базы, `Грудь/Спина/Ноги/…` для мышцы, `подсобка`, время отдыха, моношрифтовые чипы схемы `3×5 @ 85%`). Кнопка «Посмотреть программу» между графиком прогресса и action-кнопками.
- **Активация одним кликом:** внутри preview-модалки — sticky CTA «▶ Использовать эту программу» с фирменным градиентом. Клик → `navigate("/programs", { state: { assignTemplateId } })`. В `Programs.js` добавлен useEffect, который ловит state и: (a) для `requires_maxes=true` — открывает `ProgramConfigModal` для ввода 1ПМ+дней; (b) для программ без %1ПМ — сразу активирует с дефолтными днями (Пн/Ср/Пт для 3, Пн/Вт/Чт/Пт для 4, и т.д.) и уходит на Home. Проверено скриншотами: preview с CTA внизу + автомодалка на `/programs`.
- **Fullscreen на скролле (bottom-sheet ↔ fullscreen):** `.app-sheet` стартует как bottom-sheet (92vh, скругления, «хват»). Скролл `> 48px` → добавляется класс `.fullscreen` → плавно (transition 0.4s cubic-bezier) разворачивается до 100dvh, без скруглений, с sticky glass-header (blur backdrop, X-кнопка всегда доступна). Скролл обратно `< 6px` → sheet сворачивается. На desktop (>768px) — max-width 640→760, безрамочное расширение. Проверено скриншотами: sheet → fullscreen → deep scroll → back.
- **Багфиксы (iter7):** (1) `_exercise_doc` в seed.py теперь ставит `weight_type='bodyweight'/'kg'` в зависимости от equipment + при старте backend `update_many` бэкфилит существующие БД-записи + `weight_type` добавлен в Pydantic Exercise model. (2) `_ai_build_template` берёт weight_type из каталога при матче — теперь bodyweight-упражнения (планка, подтягивания) сохраняют bodyweight, а гантельные/штанговые получают kg. Frontend `WorkoutView.js::PlanRows` показывает «Свой вес» только для `weight_type='bodyweight'`, иначе — «—». (3) `_AI_SYSTEM_GEN` обогащён 10 референсными программами (Wendler 5/3/1, Sheiko, StrongLifts, Smolov Jr, PPL, PHUL, PHAT, GVT, DUP, Bro-split) с конкретными схемами подходов×повторов и явным требованием прогрессировать sets/reps между неделями (не только %1ПМ). Тесты: 31/31 PASS (27 P6 regression + 4 AI weight_type/variation).
- **Багфиксы (iter8):** (1) DELETE /programs/templates/{id} cascade-cancels active/draft plans пользователя — активная программа больше не «висит» после удаления. Frontend Programs.js локально сбрасывает activePlan. (2) POST /plans/{plan_id}/cancel: idempotent (200/403/404/401), сессии не трогает. UI-кнопка «Отменить выбор» в активной карточке с confirming-состоянием. (3) ProgramBuilder: `[data-testid="builder-ai-promo"]` — плашка с `PulsingBorder` shader-эффектом сверху, ведёт на /programs/ai. (4) Streak: `.sk-today-dot` — теперь `display: block` 8×8, чётко центрирован в круге; новая `GiftIcon` SVG-компонент (коробка+бант) с bounce+glow+sparkle анимациями в active. (5) DateSelector: `allExercisesResolved` useMemo — когда session.status='finished' И все exercises.status ∈ {done, skipped}, вместо `[data-testid="resume-button"]` показывается `[data-testid="session-info-button"]` (Info icon) → открывает info-модалку с датой, неделей плана, done/skipped counts, тоннажем, длительностью. Также tgMainVisible не показывает Telegram MainButton в этом состоянии. Тесты: 34/34 PASS (7 новых + 27 P6 regression) + 4/4 frontend visual.
- **3D-иконки (iter9):** Добавлены `/app/frontend/public/img/3d/{star-dot,fire}.png` (оптимизированы до 512×512 PNG, ~180KB каждый). (a) `ProgramBuilder` промо-плашка — `<img src="/img/3d/star-dot.png">` вместо Lucide Sparkles + новая float-анимация `pb-ai-img-float` (вращение ±3° + вертикальное покачивание). (b) `Streak` hero — `<img src="/img/3d/fire.png">` вместо Lucide Flame; при `cur > 0` активен `.sk-flame.lit` с drop-shadow оранжевого свечения и flicker анимацией (`sk-flame-flicker` — realistic scale/rotate). При `cur === 0` — приглушённая версия через `grayscale + brightness`.
- **AI Import hero (iter10):** Начальная страница ввода промпта получила премиальный hero-блок в стиле «liquid surface»: `MeshGradient` shader (distortion 0.95, swirl 0.65, grainMixer 0.28, оранжево-чёрная палитра) как жидкий переливающийся фон + 3D `star-dot.png` иконка с float-анимацией по центру + заголовок «Программа под вас за 1 минуту» на шрифте **Playfair Display** (Google Font, полная поддержка кириллицы; medium 500 для основного текста, italic 500 для accent-чипа на peach-фоне) + подзаголовок про научные исследования + glass-badges (Wendler 5/3/1, Sheiko, PPL/PHUL/PHAT, «и ещё 7 схем»). Hero показывается только на initial-state (не при busy/questions/result).

## Тестовые аккаунты
См. `/app/memory/test_credentials.md`.

## Июль 2026 (continuation) — Редизайн «Тренировочной серии»: GitHub-календарь с сериями
- **Бэкенд** (`_athlete_streak_data`, `GET /api/stats/{tg}/streak`) — аддитивно: `streaks[]` (серии >=2 дней подряд: start/end/length/is_current/is_best, свежие первыми, cap 50), аннотация дней сетки `calendar[].days[]` → `streak_len/streak_start/streak_end`, метрики `this_month`, `avg_per_week`, `first_workout_date`. Протестировано deep_testing_backend_v2 — все сценарии PASS (авторизация 401/403, clamp weeks 1..26, пустой пользователь).
- **Фронтенд** (`Streak.js`/`Streak.css`): (1) календарь как в GitHub — над колонками ярлыки серий `🔥N` (рекорд — градиентный, текущая — pulse), дни серии соединены в вертикальные «пилюли» (run-start/run-end радиусы + conn-мостик через 4px gap); (2) тоггл периода «3 мес / 6 мес» (weeks 13/26) с автопрокруткой к свежим неделям и sticky-ярлыками дней недели; (3) новый блок «Мои серии» — список серий с диапазоном дат (fmtRange RU), тегами «сейчас»/«рекорд» (Trophy), кнопка «Показать все» при >5; (4) мини-статистика: в этом месяце / в среднем в нед / активных дней; (5) тултипы ячеек: дата + кол-во + длина серии.
- **Демо:** `streakdemo@twb.dev` / `password123` (tg 992689326272) — серии 5/3/2 дн; пересоздать `python /app/scripts/setup_streak_demo.py`.
- **Инфраструктура:** `frontend/.env` REACT_APP_BACKEND_URL обновлён на актуальный preview-домен контейнера (fc46ec61-...).

## Июль 2026 (эта сессия) — Уточнения/правки программы через ИИ (refine)
- **Бэкенд** (`server.py`): `POST /api/ai/program/refine` (`{template_id, feedback}`) — фоновая задача (`ai_jobs`, kind `refine`, polling через существующий `GET /ai/program/jobs/{id}`). Валидация: feedback ≥5 символов (400), ИИ настроен (503), программа существует (404), владелец и не builtin (403). Работает для любых ИИ-программ (генерация + разбор текста/файла).
- Новый `_template_to_ai_json(doc)` — обратная сериализация шаблона в ИИ-схему (проценты %1ПМ восстанавливаются из weight при requires_maxes+lift_group+slug в default_one_rep_max). `_ai_build_template(..., replace=doc)` — обновление программы НА МЕСТЕ: тот же id/created_at/slug/share_code/tags, `update_one` вместо insert; name/description сохраняются, если ИИ их не вернул. Промпт: полный JSON текущей программы + правки пользователя + инструкция менять только запрошенное.
- **Фронтенд** (`AiImport.js`/`.css`, `api.js::aiRefineProgram`): на экране результата (под «Посмотреть программу») блок `[data-testid="ai-refine"]`: заголовок «Что-то не так? Напишите ИИ, что исправить», textarea `ai-refine-input`, кнопка `ai-refine-btn` (disabled <5 символов). `run(fn, kind)` — для refine результат не сбрасывается при ошибке, свои busy-шаги (REFINE_BUSY_STEPS «Вносим изменения…»), toast «Программа обновлена», feedback очищается после успеха. Можно уточнять многократно подряд.
- **Тестирование:** python-тест логики (round-trip %1ПМ, in-place update, id/created_at сохранены, без дублей в БД) — PASS; curl-валидация 400/503 — PASS; Playwright E2E с mock ИИ-ответов (блок виден, disabled-состояние, busy overlay, обновлённое имя, очистка поля) — PASS. ⚠️ Реальный вызов ИИ не проверен: в preview-окружении не задан AI_API_KEY (ИИ-функция целиком «не подключена» — это относится и к генерации).

## Июль 2026 — ИИ подключён (Google Gemini)
- В `backend/.env` заданы: `AI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai` (OpenAI-совместимый эндпоинт Google), `AI_API_KEY` (ключ пользователя из AI Studio), `AI_MODEL=gemini-flash-latest`. Код `_ai_chat` не менялся — совместимость полная (Bearer + chat/completions + response_format).
- Реальный e2e через Gemini: 9 уточняющих вопросов ✅, генерация программы (DUP, 2 нед × 3 дня) ✅, refine («убери становую → румынская») — тот же id, правка применена ✅. Тестовая программа из БД удалена.
